// import { useUserPermission } from '@/composables/core/permission'

// const { currentUserPermission, fetchUserPermission } = useUserPermission()
// const alreadyLoaded = ref(false)

// export default function isAuthenticated(route:any) {
//     if (!alreadyLoaded.value) {
//         fetchUserPermission()
//         alreadyLoaded.value = true
//     }
// }
